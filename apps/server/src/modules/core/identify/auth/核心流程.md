## 核心流程

登录流程:
用户密码验证 → 创建 Session → 创建设备记录 → 生成 AT + RT → 返回客户端

刷新流程:
RT 验证 → 检查是否已使用/撤销 → 重放检测 → 标记旧 RT 为已使用 → 生成新 AT + 新 RT → 更新 Session 活跃时间

登出流程:
撤销当前 RT → 可选：撤销整个 Session 的所有 RT

## Redis 键设计

## 路由设计
